<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.indoortracking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IndoorTracking">
        <activity android:name=".tracking.wifi.wifi"></activity>
        <activity android:name=".tracking.triangulation.triangulation" />
        <activity
            android:name=".tracking.map.map"
            android:label="@string/title_activity_map"
            android:theme="@style/Theme.IndoorTracking.NoActionBar" />
        <activity android:name=".tracking.imu.imu" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.IndoorTracking.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>